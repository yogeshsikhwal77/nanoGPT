# yaml file for supervised fine tuning---

model_path: checkpoints/base_model_15M.pt
data_path: data/sft/aligned_pairs.json
save_path: checkpoints/sft_model_final.pt
batch_size: 8
grad_accum: 2
lr: 2.0e-4
epochs: 3