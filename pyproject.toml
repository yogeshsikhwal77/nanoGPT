[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nanogpt"
version = "0.1.0"
dependencies = [
    "torch",
    "tokenizers",
    "pyyaml",
    "fastapi",
    "uvicorn",
    "tqdm"
]